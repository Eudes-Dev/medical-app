/**
 * Hooks React personnalisés pour l'application médicale.
 *
 * Ce fichier exporte tous les hooks personnalisés de l'application.
 * Chaque hook est documenté avec ses paramètres, retours et exemples d'utilisation.
 *
 * Organisation:
 * - Hooks généraux exportés depuis ce fichier
 * - Hooks complexes dans des fichiers séparés (ex: useCalendar.ts)
 *
 * @module hooks
 */

// ============================================
// HOOKS À IMPLÉMENTER
// ============================================

/**
 * Les hooks suivants seront implémentés dans les stories futures:
 *
 * Story 3.1 (State Management Zustand):
 * - useCalendarStore: Gestion du calendrier et des créneaux
 * - usePatientStore: Gestion des données patients
 *
 * Story 2.1 (Dashboard):
 * - usePatients: Hook pour charger/filtrer les patients
 *
 * Story 4.2 (Réservation):
 * - useBooking: Hook pour le flux de réservation
 * - useAvailableSlots: Hook pour les créneaux disponibles
 */

// Placeholder export pour éviter l'erreur "module has no exports"
export {};
