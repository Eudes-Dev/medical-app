# Story 2.1: Dashboard & Liste des Patients

## Status
Ready for Review 🔍

## Story
**As a** praticien,
**I want** voir une liste interactive de tous mes patients avec recherche et pagination,
**so that** je puisse retrouver rapidement les informations d'un patient.

## Acceptance Criteria
1. Le dashboard `/dashboard` affiche les statistiques du jour (nombre de RDV, prochains RDV)
2. La page `/dashboard/patients` affiche une table interactive avec tous les patients
3. La table utilise les composants Shadcn Data Table
4. La recherche par nom/prénom/email filtre les résultats en temps réel
5. La pagination permet de naviguer entre les pages de résultats (10 patients par page)
6. Chaque ligne permet d'accéder à la fiche détaillée du patient
7. Un skeleton de chargement s'affiche pendant le fetch des données

## Tasks / Subtasks
- [x] Task 1: Layout Dashboard (AC: 1)
  - [x] Modifier `/app/dashboard/page.tsx` existant pour ajouter les statistiques
  - [x] Créer un composant `DashboardStats` dans `/components/dashboard/dashboard-stats.tsx` affichant:
    - Nombre de RDV aujourd'hui (compte des appointments avec startTime dans la journée)
    - Prochains RDV (liste des 5 prochains appointments avec status CONFIRMED ou PENDING, triés par startTime)
  - [x] Créer une Server Action `getDashboardStats()` dans `/app/dashboard/actions.ts` pour récupérer les statistiques
  - [x] Utiliser Prisma pour interroger la table `appointments` avec filtres appropriés
- [x] Task 2: Structure de la page patients (AC: 2)
  - [x] Créer `/app/dashboard/patients/page.tsx`
  - [x] Créer le layout avec header "Gestion des Patients" utilisant les composants Shadcn (Card, CardHeader, CardTitle)
  - [x] Ajouter un bouton "Nouveau Patient" (pour story 2.2) avec Shadcn Button
- [x] Task 3: Composant Data Table (AC: 3)
  - [x] Installer les dépendances: `@tanstack/react-table`
  - [x] Ajouter les composants Shadcn nécessaires: `npx shadcn@latest add table`
  - [x] Créer `/components/patients/patient-data-table.tsx`
  - [x] Définir les colonnes avec `@tanstack/react-table`: Nom (lastName), Prénom (firstName), Email, Téléphone (phone), Actions
- [x] Task 4: Server Action pour récupérer les patients (AC: 2)
  - [x] Créer `/app/dashboard/patients/actions.ts`
  - [x] Implémenter `getPatients(page: number, limit: number, search?: string)` avec Prisma
  - [x] Utiliser `prisma.patient.findMany()` avec pagination (skip/take) et filtrage conditionnel
  - [x] Utiliser `prisma.patient.count()` pour obtenir le total (avec les mêmes filtres) pour la pagination
  - [x] Retourner un objet `{ patients, total }` pour permettre la pagination côté client
- [x] Task 5: Recherche (AC: 4)
  - [x] Ajouter un champ de recherche avec Shadcn Input
  - [x] Implémenter le filtrage côté serveur (nom, prénom, email)
  - [x] Utiliser un debounce de 300ms pour éviter les requêtes excessives
- [x] Task 6: Pagination (AC: 5)
  - [x] Implémenter la pagination avec `@tanstack/react-table`
  - [x] Afficher 10 patients par page
  - [x] Ajouter les contrôles de navigation (précédent, suivant, numéros de page)
- [x] Task 7: Navigation vers fiche patient (AC: 6)
  - [x] Ajouter un lien/bouton "Voir" sur chaque ligne
  - [x] Naviguer vers `/dashboard/patients/[id]`
- [x] Task 8: Skeleton de chargement (AC: 7)
  - [x] Créer un composant `PatientTableSkeleton`
  - [x] Afficher le skeleton pendant le chargement initial
  - [x] Utiliser Suspense de React pour le loading state

## Dev Notes

### Architecture de l'Information
- `/dashboard`: Vue d'ensemble (statistiques du jour, prochains RDV)
- `/dashboard/patients`: Gestion de la base patient (recherche, fiches détaillées)
[Source: docs/front-end-spec/1-architecture-de-linformation-sitemap.md]

### Logique de Données
- **Relations**: Patient (1) <---> (N) Appointment
- **Accès**: Couche de services via Next.js Server Actions
- **Schéma Prisma**: Le modèle `Patient` contient les champs suivants:
  - `id`: UUID (généré automatiquement)
  - `firstName`: String (mappé à `first_name` en base)
  - `lastName`: String (mappé à `last_name` en base)
  - `phone`: String (obligatoire)
  - `email`: String? (optionnel)
  - `dateOfBirth`: DateTime? (optionnel)
  - `notes`: String? (optionnel)
  - `createdAt`: DateTime
  - `updatedAt`: DateTime
  - `appointments`: Relation vers Appointment[]
- **Note**: Les champs Prisma utilisent camelCase (`firstName`, `lastName`) mais sont mappés en snake_case en base (`first_name`, `last_name`)
- **Pour les statistiques du dashboard**: Utiliser le modèle `Appointment` avec:
  - `startTime`: DateTime (pour filtrer les RDV du jour)
  - `endTime`: DateTime
  - `status`: AppointmentStatus (PENDING, CONFIRMED, CANCELLED, COMPLETED)
  - `patientId`: UUID (relation vers Patient)
[Source: docs/architecture/2-logique-de-donnes.md, prisma/schema.prisma]

### Design System
- Utiliser les composants Shadcn UI (Table, Input, Button)
- Couleur principale: Blue-600 (#2563eb)
- Couleur neutre pour le texte: Slate-900 (#0f172a)
[Source: docs/front-end-spec/2-design-system-ui-components.md]

### Requête Prisma exemple
```typescript
// Server Action getPatients dans /app/dashboard/patients/actions.ts
import { prisma } from '@/lib/prisma';

export async function getPatients(
  page: number = 1,
  limit: number = 10,
  search?: string
) {
  const where = search
    ? {
        OR: [
          { firstName: { contains: search, mode: 'insensitive' as const } },
          { lastName: { contains: search, mode: 'insensitive' as const } },
          { email: { contains: search, mode: 'insensitive' as const } },
        ],
      }
    : {};

  const [patients, total] = await Promise.all([
    prisma.patient.findMany({
      where,
      skip: (page - 1) * limit,
      take: limit,
      orderBy: { lastName: 'asc' },
    }),
    prisma.patient.count({ where }),
  ]);

  return { patients, total };
}
```

**Note importante**: 
- Le filtrage OR n'est appliqué que si `search` est fourni (non vide)
- Utiliser `mode: 'insensitive'` pour une recherche insensible à la casse
- Le `total` est nécessaire pour calculer le nombre de pages dans la pagination
[Source: prisma/schema.prisma]

### Testing
- Vérifier l'affichage correct de la table avec des données
- Tester la recherche avec différents critères
- Vérifier la pagination (navigation, nombre de résultats)
- Tester l'affichage du skeleton pendant le chargement

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5 (via Cursor)

### Debug Log References
Aucun problème bloquant rencontré.

### Completion Notes List
- Tous les Acceptance Criteria sont implémentés
- Le dashboard affiche maintenant les statistiques réelles depuis la base de données (RDV aujourd'hui et prochains RDV)
- La page `/dashboard/patients` affiche une table interactive avec recherche et pagination
- La recherche utilise un debounce de 300ms pour éviter les requêtes excessives
- La pagination fonctionne avec 10 patients par page
- Chaque ligne de la table contient un bouton "Voir" pour naviguer vers la fiche patient (route `/dashboard/patients/[id]` - à implémenter dans story 2.2)
- Un skeleton de chargement s'affiche pendant le chargement des données
- Tous les composants sont bien commentés pour faciliter la compréhension du code
- Le linting et TypeScript passent sans erreur

### File List
| File | Action | Description |
|------|--------|-------------|
| `app/dashboard/actions.ts` | Created | Server Action getDashboardStats() pour récupérer les statistiques du dashboard |
| `components/dashboard/dashboard-stats.tsx` | Created | Composants TodayAppointmentsCard et UpcomingAppointmentsCard pour afficher les statistiques |
| `app/dashboard/page.tsx` | Modified | Intégration des composants DashboardStats pour afficher les statistiques réelles |
| `app/dashboard/patients/page.tsx` | Created | Page de gestion des patients avec layout et bouton "Nouveau Patient" |
| `app/dashboard/patients/actions.ts` | Created | Server Action getPatients() avec pagination et recherche |
| `components/patients/patient-data-table.tsx` | Created | Composant Data Table avec @tanstack/react-table |
| `components/patients/patients-table-wrapper.tsx` | Created | Wrapper client pour gérer l'état de recherche et pagination |
| `components/patients/patient-table-skeleton.tsx` | Created | Composant skeleton de chargement pour la table |
| `hooks/use-debounce.ts` | Created | Hook personnalisé pour le debounce de la recherche |
| `components/ui/table.tsx` | Created | Composant Shadcn UI Table (via npx shadcn) |
| `package.json` | Modified | Ajout dépendance @tanstack/react-table |

## QA Results

### Review Date: 2026-01-29

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

L'implémentation de la story 2.1 est fonctionnelle et bien structurée. Le code suit les bonnes pratiques Next.js avec une séparation claire entre Server Components, Client Components et Server Actions. La documentation est complète avec des commentaires JSDoc détaillés. Les requêtes Prisma sont optimisées avec `Promise.all` pour les requêtes parallèles et utilisent correctement la pagination et les filtres.

**Points forts:**
- Architecture claire avec séparation Server/Client Components
- Code bien documenté avec JSDoc
- Requêtes Prisma optimisées (Promise.all, pagination efficace)
- Utilisation appropriée du debounce pour la recherche (300ms)
- Gestion correcte des états de chargement avec skeleton
- Intégration propre avec Shadcn UI

**Points à améliorer:**
- Absence totale de tests automatisés (critique)
- Gestion d'erreurs d'authentification pourrait être plus explicite
- Validation des entrées utilisateur côté serveur à renforcer
- Gestion d'erreurs Prisma non explicite

### Refactoring Performed

Aucun refactoring n'a été effectué lors de cette revue. Le code est déjà bien structuré et suit les patterns appropriés. Les améliorations suggérées sont documentées dans les recommandations ci-dessous.

### Compliance Check

- Coding Standards: ✓ Code suit les conventions TypeScript/Next.js, bien formaté
- Project Structure: ✓ Fichiers organisés selon la structure du projet
- Testing Strategy: ✗ Aucun test n'a été implémenté (voir test design pour recommandations)
- All ACs Met: ✓ Tous les critères d'acceptation sont implémentés fonctionnellement

### Improvements Checklist

- [ ] Ajouter des tests automatisés selon le test design (docs/qa/assessments/2.1-test-design-20260129.md)
- [ ] Améliorer la gestion d'erreurs d'authentification dans les Server Actions (lever une erreur explicite au lieu de retourner null/[])
- [ ] Ajouter validation Zod pour le paramètre search dans getPatients
- [ ] Ajouter gestion d'erreurs explicite avec try-catch dans les Server Actions
- [ ] Considérer l'ajout d'un système de cache pour les requêtes fréquentes (future optimisation)

### Security Review

**Statut: CONCERNS**

L'authentification est vérifiée dans les Server Actions via Supabase Auth, et le middleware protège les routes `/dashboard/*`. Cependant:

1. **Gestion d'erreurs non authentifié**: Les Server Actions retournent `null` ou `[]` au lieu de lever une erreur explicite. Cela peut masquer des problèmes de sécurité et rendre le debugging plus difficile.

2. **Validation des entrées**: Le paramètre `search` n'est pas validé/sanitisé côté serveur. Bien que Prisma utilise des requêtes paramétrées (protection contre SQL injection), une validation de longueur et de caractères autorisés serait recommandée.

3. **Protection contre les attaques**: Pas de rate limiting visible sur les endpoints de recherche (à considérer pour la production).

**Recommandations:**
- Lever une `UnauthorizedError` explicite dans les Server Actions si l'utilisateur n'est pas authentifié
- Ajouter une validation Zod pour le paramètre search (longueur max, caractères autorisés)
- Considérer l'ajout de rate limiting pour les endpoints de recherche (story future)

### Performance Considerations

**Statut: PASS**

Les performances sont bien gérées:

1. **Requêtes Prisma optimisées**: Utilisation de `Promise.all` pour exécuter les requêtes en parallèle (patients + count)
2. **Pagination efficace**: Utilisation correcte de `skip`/`take` pour limiter les résultats
3. **Debounce de recherche**: 300ms de délai pour éviter les requêtes excessives
4. **Index Prisma**: Le schéma Prisma définit des index sur `patientId` et `startTime` pour optimiser les requêtes

**Recommandations futures:**
- Considérer l'ajout d'un cache Redis pour les requêtes fréquentes (dashboard stats)
- Surveiller les performances avec un grand nombre de patients (>10,000)
- Ajouter des métriques de performance pour surveiller les requêtes Prisma

### Files Modified During Review

Aucun fichier n'a été modifié lors de cette revue. Les recommandations sont documentées ci-dessus et dans le fichier gate.

### Gate Status

Gate: **CONCERNS** → `docs/qa/gates/2.1-dashboard-liste-patients.yml`
Test design: `docs/qa/assessments/2.1-test-design-20260129.md`

**Décision du gate:** CONCERNS - L'implémentation est fonctionnelle et bien structurée, mais l'absence de tests automatisés et quelques préoccupations de sécurité doivent être adressées avant la mise en production.

**Score de qualité:** 70/100
- Points déduits: -20 (absence de tests), -10 (préoccupations sécurité)

### Recommended Status

✗ **Changes Required** - Voir les éléments non cochés dans la checklist ci-dessus

**Actions requises avant "Ready for Done":**
1. Ajouter des tests automatisés (au minimum les tests P0 du test design)
2. Améliorer la gestion d'erreurs d'authentification
3. Ajouter validation des entrées utilisateur

**Note:** Le code fonctionne correctement et tous les ACs sont implémentés. Les préoccupations sont principalement liées à la qualité, la sécurité et la maintenabilité à long terme.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-25 | 1.0 | Création initiale de la story | SM Agent (Bob) |
| 2026-01-29 | 1.1 | Validation PO: Correction des chemins de fichiers, ajout sections manquantes (Dev Agent Record, QA Results), amélioration Dev Notes avec référence schéma Prisma, amélioration section Testing, correction requête Prisma avec gestion search vide | PO Agent (Sarah) |
| 2026-01-29 | 1.2 | Implémentation complète du dashboard et de la liste des patients avec recherche et pagination | DEV Agent (James) |
