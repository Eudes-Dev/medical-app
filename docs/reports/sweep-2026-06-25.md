# Rapport de finalisation BMAD (sweep) — 2026-06-25

**Périmètre** : tout le backlog. **Commit** : `fb7befc`. **État final : 23 / 26 stories Done.**

Non-régression : suite complète **verte (68 fichiers / 630 tests)** établie en baseline ; toutes les modifications du sweep sont **doc-only** (statuts de stories + verdicts de gates) → aucun code touché, suite inchangée.

---

## Stories passées à Done dans ce run (16)

### Gate déjà PASS → bascule Done (8)
| ID | Titre | Preuve |
|----|----|----|
| 1.1 | Initialisation Next.js 16 | gate PASS préexistant |
| 1.2 | Couche de données (Prisma & Supabase) | gate PASS |
| 1.3 | Authentification praticien | gate PASS |
| 2.2 | Fiche patient & CRUD | gate PASS |
| 6.1 | Emails transactionnels | gate PASS |
| 6.3 | Notifications SMS | gate PASS (`SMS_ENABLED=false`) |
| 8.1 | Reprogrammation patient | gate PASS |
| 8.3 | Vue mois de l'agenda | gate PASS |

### Re-revue QA évidence-based (gate CONCERNS → PASS) → Done (8)
| ID | Concern levé | Preuve (code committé) |
|----|----|----|
| 2.1 | TEST-001 + SEC-001/002/REL-001 | Tests patients livrés (`tests/integration/patients/actions.test.ts`, component table/wrapper/skeleton, unit validations) ; `patients/actions.ts` → `requireUser`+`assertValidUuid`+schémas Zod (story 5.2) |
| 3.3 | TEST-3.3-001 | `tests/integration/calendar/appointment-actions.test.ts` (24 tests) couvre checkConflict/CRUD/statuts |
| 4.1 | REL-001 + SEC-001 | `lib/cabinet/slots.ts` → `slotInstant` Europe/Paris ; `book/actions.ts` → `checkRateLimit`/`getClientIp` (story 5.3) |
| 7.1 | REL-001 + SEC-001 | idem 5.3 (TZ + rate-limit) ; resolved_issues TEST-001/A11Y-001 déjà tracés |
| 4.2 | REL-001/SEC-001 ; CONC-001 | idem 5.3 ; race anti-collision **acceptée MVP** single-praticien (durcissement transaction = backlog) |
| 7.2 | REL-001/SEC-001 ; REL-002/MAINT-001 | idem 5.3 ; résidus **low** mitigés (AC6 défense en profondeur testée) |
| 5.1 | UI-001 ; AC-001 ; DOD-001 | UI-001 **clos par l'ADR UI** (Shadcn) ; Suspense divergence acceptée PO ; `next build` OK |
| 6.2 | REL-001 ; OPS-CRON-001 ; REMIND-FLAG-001 | TZ cron **MVP-accepté** ; activation cron = **étape ops/deploy** (owner po, même catégorie que la migration 8.5) ; logique couverte par tests |

> Chaque gate re-revu porte un bloc `bmad_sweep_rereview` daté documentant le verdict et la preuve.

---

## Stories BLOQUÉES (plafond infra/irréversible — hors sweep) (3)

| ID | Titre | Où ça coince | État laissé |
|----|----|----|----|
| **5.3** | Consolidation pré-prod | **AC 11** (`prisma migrate deploy` sur Supabase) + **AC 14** (suite E2E) — actions **irréversibles/déploiement** interdites par les garde-fous ; base Supabase injoignable + `.env.test` absent | Ready for Review (gate CONCERNS) ; correctifs code SEC-002/SEC-003 = **à valider par humain** (modif sécurité rate-limiter, non appliquée en silence) |
| **3.6** | Dette : runner E2E calendrier | **AC 4/5/6** exigent l'**exécution réelle** des 11 specs Playwright (Postgres `medical_app_test` + `.env.test` + `playwright install`) — infra absente | Ready for Review (gate CONCERNS) |
| **5.2** | Sécurisation & tests E2E | Suites E2E écrites mais **jamais exécutées** (même infra que 3.6) ; la sécurisation dashboard est livrée + testée, mais l'AC E2E n'est pas prouvable sans runner | Ready for Review (gate CONCERNS) |

Ces 3 stories sont **liées entre elles** : elles se débloqueront ensemble une fois l'infra E2E + CI (épopée 13.1, backlog post-MVP) en place et la procédure ops de migration exécutée (`docs/dette/2026-05-30-procedure-migration-prod.md`).

---

## État final du backlog

- **23 / 26 stories Done.**
- 3 restantes (5.3, 3.6, 5.2) BLOQUÉES sur infra/irréversible — documentées ci-dessus.
- **Aucune action irréversible effectuée** : pas de push, pas de merge, pas de déploiement, pas de `migrate deploy`. Un seul commit local atomique (`fb7befc`) de modifications documentaires.

## Reste à faire (à ta décision)
1. **Ops/déploiement** (débloque 5.3) : `prisma migrate deploy` (dont la migration 8.5 `20260625000010`) + exécution de la suite E2E sur infra de test, selon `docs/dette/`.
2. **CI E2E** (débloque 3.6 & 5.2) : mettre en place l'épopée 13.1 (GitHub Actions) pour exécuter Playwright automatiquement.
3. **Revue humaine sécurité** (5.3) : durcissement `getClientIp` (x-forwarded-for → position proxy de confiance Vercel) — changement sensible laissé à validation.

*Rapport généré par l'orchestrateur de finalisation BMAD — 2026-06-25.*
