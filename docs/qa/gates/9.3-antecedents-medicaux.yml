# Quality Gate — Story 9.3 : Antécédents médicaux
# Généré par Quinn (Test Architect) via *review.
schema: 1
story: "9.3"
story_title: "Antécédents médicaux (fond clinique structuré et catégorisé du dossier patient)"
gate: PASS
status_reason: >-
  8/8 AC couverts. Modèle MedicalHistoryEntry + enum MedicalHistoryCategory + migration non
  destructive (CREATE TYPE/TABLE/INDEX + FK Cascade) ; 4 Server Actions sécurisées
  (requireUser + assertValidUuid + Zod isomorphe content ≤ 2000 + enum) ; UI fiche patient
  regroupée par catégorie (ajout / édition contenu+catégorie / suppression confirmée).
  Suite complète verte (74 fichiers / 689 tests, +19 vs baseline, aucune régression). Build OK.
reviewer: "Quinn (Test Architect)"
updated: "2026-06-25T16:45:00Z"

waiver:
  active: false

quality_score: 95

top_issues:
  - id: RGPD-PROD-931
    severity: medium
    finding: >-
      Données de santé (art. 9 RGPD), comme les notes (9.1) et documents (9.2). Le socle est
      sécurisé (requireUser, assertValidUuid, Zod, Prisma paramétré, aucune surface publique)
      mais le chiffrement applicatif (11.4), l'audit des accès (11.3) et le consentement/droit
      à l'oubli (11.1/11.2) ne sont pas livrés.
    status: accepted_by_design
    mitigation: >-
      Mise en PRODUCTION de tout le dossier patient conditionnée à un arbitrage PO/RGPD
      (épopée 11). Réserve documentée dans la story (Dev Notes) et cohérente avec 9.2.
      La story reste développable/testable en dev/preview.

quality_score_notes: >-
  95 (−5 pour la réserve RGPD-PROD-931, organisationnelle/produit et non un défaut de code).

evidence:
  tests_reviewed: 19
  tests_status: "19/19 verts sur le périmètre 9.3 (unit 6, integration 13)"
  full_baseline: "74 fichiers / 689 tests verts (baseline 9.2 = 72/670 + 2 fichiers / 19 tests 9.3)"
  risk_summary: >-
    Risque technique faible : feature additive single-tenant, orthogonale au cœur agenda ;
    migration non destructive ; pas de fichier/binaire (données structurées) ; patron repris
    de 9.1. Risque de conformité (RGPD) porté hors code par RGPD-PROD-931.
  trace:
    ac_covered: [1, 2, 3, 4, 5, 6, 7, 8]
    ac_gaps: []
    notes: >-
      AC1/AC8 (modèle, cascade patient) validés au niveau FK schéma+migration (non
      unit-testables sans base, cohérent avec 8.5/9.1/9.2). AC4/AC5/AC6 (UI : regroupement par
      catégorie, ajout, édition inline contenu+catégorie, suppression confirmée) vérifiés par
      inspection — le rendu n'est pas unit-testé (politique identique à 9.1/9.2) mais la logique
      sous-jacente (actions + validation) est couverte. Cohérence du regroupement après
      changement de catégorie en édition vérifiée (re-filtrage par catégorie au rendu).

nfr_validation:
  security:
    status: PASS
    notes: >-
      `requireUser()` en tête des 4 actions (testé : non-auth → UnauthorizedError / échec sans
      Prisma). `assertValidUuid()` sur tout id avant Prisma (testé : UUID invalide → []/échec sans
      Prisma). Validation Zod isomorphe (content trimé non vide ≤ 2000, catégorie ∈ enum ; rejet
      sans Prisma testé). Prisma paramétré ; aucune surface publique ni fichier.
  performance:
    status: PASS
    notes: >-
      Index `medical_history_entries(patient_id)`. Tri `createdAt desc` en base. Regroupement par
      catégorie en O(catégories × entrées) côté client sur une volumétrie patient faible
      (single-tenant). Aucun appel réseau superflu.
  reliability:
    status: PASS
    notes: >-
      Forme de retour discriminée { success } homogène avec 9.1 ; erreurs mappées FR sans fuite
      d'id. Édition met à jour l'état local en place (migration de groupe automatique au rendu) ;
      création prepend (ordre desc préservé). Cascade FK : suppression patient purge les antécédents.
  maintainability:
    status: PASS
    notes: >-
      Patterns du dépôt respectés (actions calquées sur consultation-note-actions, harnais de test
      identique à consultation-notes-actions). Validation pure (testable unit, aucun import
      Prisma/Supabase) ; libellés de catégories réutilisables (MEDICAL_HISTORY_CATEGORY_LABELS) ;
      messages via TOAST_MESSAGES.medicalHistory ; JSDoc soignée.

recommendations:
  immediate:
    - action: "OPS : appliquer la migration 20260625000013_add_medical_history_entries en base (étape de déploiement standard, non exécutée par la boucle)."
      refs: ["prisma/migrations/20260625000013_add_medical_history_entries/migration.sql"]
    - action: "PO/RGPD : intégrer les antécédents au même arbitrage d'exposition production que 9.1/9.2 (RGPD-PROD-931) avant usage sur de vraies données patients."
      refs: ["docs/stories/9.3.antecedents-medicaux.story.md", "docs/architecture/5-stockage-fichiers-decision.md"]
  future:
    - action: "Épopée 11 : chiffrement applicatif (11.4), journal d'audit des accès (11.3), consentement/droit à l'oubli/export (11.1/11.2) couvrant l'ensemble du dossier patient (notes + documents + antécédents)."
      refs: ["docs/stories/ROADMAP.md"]
    - action: "Couverture E2E (Playwright) du parcours antécédents une fois l'infra E2E disponible (cf. 3.6/5.2)."
      refs: ["tests/e2e/"]
